#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
//	scrivi un programma che visualizzi i primi 100 numeri dispari a gruppi di 5
int dispari,
int gruppi;
for(dispari=1;dispari<100;dispari=dispari+2);
{
	for(gruppo=1;gruppo<=5;gruppo++)
	{
		printf("numero dispari");
		scanf("%d",&dispari);
		dispari=dispari+2;
	}
	printf("gruppo uguale a %d";gruppo);
}
return 0;
	
}